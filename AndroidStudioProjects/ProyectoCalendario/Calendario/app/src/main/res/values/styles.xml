<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Widget.CalendarView.Custom" parent="android:Widget.CalendarView">
        <item name="android:focusedMonthDateColor">@color/colorAccent</item>
        <item name="android:weekNumberColor">#d23737</item>
        <item name="android:selectedWeekBackgroundColor">#000</item>
        <item name="android:weekDayTextAppearance">@style/TextAppearance.MaterialCalendarWidget.WeekDay</item>
        <item name="android:dateTextAppearance">@style/TextAppearance.MaterialCalendarWidget.WeekDay</item>
    </style>

    <style name="CustomDayTextAppearance"
        parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@color/mcv_text_date_dark</item>
    </style>

</resources>
